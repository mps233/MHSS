# TMDB API Key - 从 https://www.themoviedb.org/settings/api 获取
TMDB_API_KEY=your_tmdb_api_key_here

# Emby 配置（用于登录认证和显示影片库数量）
# EMBY_URL: Emby 服务器地址，例如 http://192.168.1.100:8096
# EMBY_API_KEY: 在 Emby 设置 -> API 密钥中生成
EMBY_URL=http://your-emby-server:8096
EMBY_API_KEY=your_emby_api_key_here

# MediaHelper 配置（必需，用于创建订阅）
MEDIAHELPER_URL=http://your-mediahelper-url:7080
MEDIAHELPER_USERNAME=admin
MEDIAHELPER_PASSWORD=admin

# MediaHelper 云盘类型（可选，系统会自动从 MediaHelper 获取）
# 如需覆盖自动识别，可手动配置：drive115, tianyiyun, quark, aliyundrive
# MEDIAHELPER_CLOUD_TYPE=drive115

# 服务器端口
PORT=3000

# HTTP 代理配置（可选）- 用于访问 TMDB API
# 如果在中国大陆访问 TMDB 较慢，可以配置代理
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# HDHive 配置（可选）- 自动获取免费 115 分享链接
# 启用后，订阅时会自动从 HDHive 查询免费 115 链接并添加到订阅中
# 
# 推荐配置：使用账号密码（自动管理 Cookie）
# HDHIVE_USERNAME: HDHive 账号
# HDHIVE_PASSWORD: HDHive 密码
# 
# 工作原理：
# - 首次运行：自动登录获取 Cookie 并保存（需要 Playwright，约 5 秒）
# - 后续运行：直接使用保存的 Cookie（约 2 秒）
# - Cookie 过期（约 7 天）：自动刷新
# 
# 环境要求：
# - pip install playwright
# - playwright install chromium-headless-shell  # 推荐，Linux ~158MB
# 
# 备选配置：手动 Cookie（仅适用于无法安装浏览器的环境）
# HDHIVE_COOKIE: HDHive 网站的 Cookie
# 获取方法：
# 1. 登录 https://hdhive.com
# 2. 打开浏览器开发者工具（F12）
# 3. 在 Network 标签页找到任意请求
# 4. 复制 Request Headers 中的 Cookie 值
# 格式示例：token=eyJhbGc...; csrf_access_token=xxx
# 注意：Cookie 会过期（约 7 天），需要手动更新
# 
HDHIVE_ENABLED=false
# 推荐：只配置账号密码
HDHIVE_USERNAME=
HDHIVE_PASSWORD=
# 备选：手动 Cookie（不推荐，会过期）
# HDHIVE_COOKIE=

